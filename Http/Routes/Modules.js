const express = require('express');
const router = express.Router();

// Routes related to Modules

// Todo: route to get all modules

// Todo: route to get one module

// Todo: route to create module

// Todo: route to delete module

// Todo: route to edit a module

module.exports = router;
